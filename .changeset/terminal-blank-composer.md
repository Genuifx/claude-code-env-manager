---
"ccem": patch
---

Allow the desktop workspace Terminal action to launch a new session from an empty composer, and avoid passing `ANTHROPIC_API_KEY` when a managed `ANTHROPIC_AUTH_TOKEN` is present.
