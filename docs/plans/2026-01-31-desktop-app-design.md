# CCEM Desktop App 设计文档

> 将 ccem CLI 功能迁移到 Tauri 跨平台桌面应用

## 概述

构建一个功能完整的桌面应用，使用 Tauri + React + TailwindCSS + shadcn/ui，具备：

- **完整 GUI** - 所有 ccem 功能的可视化界面
- **系统托盘** - 后台常驻，快速切换环境/权限
- **多实例管理** - 同时运行多个不同环境的 Claude 会话
- **终端集成** - 支持 Terminal.app 和 iTerm2（增强体验）

## 技术选型

| 层级 | 技术 |
|------|------|
| 框架 | Tauri 2.0 |
| 前端 | React 18 + TypeScript |
| 样式 | TailwindCSS + shadcn/ui |
| 状态管理 | Zustand |
| 终端启动 | @tauri-apps/plugin-shell |
| 测试 | Vitest |

## 项目结构

```
claude-code-env-manager/
├── packages/
│   └── core/                 # 共享核心逻辑
│       ├── src/
│       │   ├── types.ts      # 类型定义
│       │   ├── presets.ts    # 环境和权限预设
│       │   ├── encryption.ts # 加密解密
│       │   └── config.ts     # 配置读写
│       └── package.json
├── apps/
│   ├── cli/                  # 现有 CLI（重构引用 core）
│   │   └── src/
│   └── desktop/              # Tauri 桌面应用
│       ├── src/              # React 前端
│       ├── src-tauri/        # Rust 后端
│       └── package.json
├── pnpm-workspace.yaml
└── package.json
```

## 系统托盘设计

```
┌─────────────────────────────────┐
│ 🟢 Claude Code Env Manager      │
├─────────────────────────────────┤
│ 当前环境: official              │
│ ├─ ● official                   │
│ ├─ ○ GLM                        │
│ ├─ ○ DeepSeek                   │
│ └─ ○ KIMI                       │
├─────────────────────────────────┤
│ 权限模式: 开发模式              │
│ ├─ ○ YOLO 模式                  │
│ ├─ ● 开发模式                   │
│ ├─ ○ 只读模式                   │
│ └─ ...                          │
├─────────────────────────────────┤
│ ▶ 启动 Claude                   │
├─────────────────────────────────┤
│ 活跃会话 (2)                    │
│ ├─ official + dev    [PID 1234] │
│ └─ GLM + yolo        [PID 5678] │
├─────────────────────────────────┤
│ 打开主窗口                       │
│ 退出                            │
└─────────────────────────────────┘
```

**交互行为：**

- 切换环境/权限：立即生效，下次启动 Claude 使用新配置
- 启动 Claude：根据设置打开 Terminal.app 或 iTerm2，注入环境变量
- 活跃会话：显示当前运行的 Claude 进程，点击可聚焦对应终端窗口

## 主窗口页面设计

### UI 风格

参考 Antigravity Tools 设计语言：

- 顶部居中 Tab 导航
- 统计卡片横向排列
- 左右分栏卡片布局
- 白底 + 绿色主色调 + 渐变按钮
- 圆角卡片带阴影

### 页面结构

| 页面 | 功能 |
|------|------|
| 仪表盘 | 当前配置概览、统计卡片、活跃会话列表、快速启动 |
| 环境管理 | 环境列表、添加/编辑/删除/复制、从预设添加、远程加载 |
| 权限模式 | 6 种模式的可视化配置和说明、设置默认模式 |
| Skills | 已安装技能列表、Tab 切换添加（官方/精选/其他） |
| 设置 | 终端选择、主题、开机启动等 |

### Dashboard 布局

```
┌──────────────────────────────────────────────────────────────────────────┐
│ ● ● ●   CCEM                                                             │
│                                                                          │
│         [仪表盘]  环境管理   权限模式   Skills   设置        ☀️  ZH      │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  你好，开发者 👋                                    [+ 添加环境] [▶ 启动] │
│                                                                          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 🌐       │ │ 💰       │ │ 📊       │ │ 🚀       │ │ ⚡        │       │
│  │ 4        │ │ $18.50   │ │ 1.2M     │ │ 2        │ │ dev      │       │
│  │ 环境数    │ │ 本月费用  │ │ Tokens   │ │ 活跃会话  │ │ 权限模式  │       │
│  │ ✓ 已配置  │ │ ↑12%     │ │ 本月用量  │ │ 运行中   │ │ 开发模式  │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
│                                                                          │
│  ┌─────────────────────────────┐  ┌─────────────────────────────┐       │
│  │ ✓ 当前环境                   │  │ 📈 活跃会话                  │       │
│  │                             │  │                             │       │
│  │ official                    │  │ ┌─────────────────────────┐ │       │
│  │ ─────────────────────────── │  │ │ official + dev          │ │       │
│  │ claude-sonnet  R: 无限 100% │  │ │ 12:30 启动   [聚焦] [停止]│ │       │
│  │ ████████████████████████    │  │ └─────────────────────────┘ │       │
│  │ claude-haiku   R: 无限 100% │  │ ┌─────────────────────────┐ │       │
│  │ ████████████████████████    │  │ │ GLM + yolo              │ │       │
│  │                             │  │ │ 14:15 启动   [聚焦] [停止]│ │       │
│  │      [ 切换环境 ]            │  │ └─────────────────────────┘ │       │
│  └─────────────────────────────┘  │                             │       │
│                                   │      [ 一键全部停止 ]        │       │
│  ┌─────────────────────────────┐  └─────────────────────────────┘       │
│  │ 查看所有环境              → │                                        │
│  └─────────────────────────────┘                                        │
│  ┌─────────────────────────────┐                                        │
│  │ 查看用量详情              ↓ │                                        │
│  └─────────────────────────────┘                                        │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘
```

## Tauri 后端架构

```
src-tauri/
├── src/
│   ├── main.rs              # 入口 + 系统托盘
│   ├── commands/
│   │   ├── env.rs           # 环境 CRUD
│   │   ├── permission.rs    # 权限模式管理
│   │   ├── session.rs       # 会话管理（启动/停止/列表）
│   │   ├── usage.rs         # 用量统计
│   │   └── skill.rs         # Skill 管理
│   ├── terminal/
│   │   ├── mod.rs           # 终端抽象层
│   │   ├── detector.rs      # 探测已安装终端
│   │   ├── terminal_app.rs  # Terminal.app 实现
│   │   └── iterm2.rs        # iTerm2 增强实现
│   ├── config.rs            # 配置文件读写
│   └── tray.rs              # 托盘菜单逻辑
```

### Tauri Commands

| Command | 功能 |
|---------|------|
| `get_environments` | 获取所有环境列表 |
| `switch_environment` | 切换当前环境 |
| `launch_claude` | 启动 Claude（指定环境+权限） |
| `list_sessions` | 获取活跃会话列表 |
| `stop_session` | 停止指定会话 |
| `get_usage_stats` | 获取用量统计 |
| `detect_terminal` | 探测可用终端 |

## 终端集成策略

### 双轨方案

| 功能 | Terminal.app（降级） | iTerm2（增强） |
|------|---------------------|----------------|
| 启动 Claude | AppleScript 打开窗口执行命令 | Python API 创建命名会话 |
| 窗口标题 | 无法设置 | 自动设为 `Claude: {env} + {mode}` |
| 聚焦窗口 | 只能激活 Terminal 应用 | 精确聚焦到指定 Tab/窗口 |
| 会话状态 | 轮询 PID 是否存活 | 监听 iTerm2 会话事件 |
| 停止会话 | 发送 SIGTERM 到 PID | 优雅关闭 iTerm2 会话 |

### iTerm2 集成示例

```applescript
-- 创建带标签的窗口
tell application "iTerm2"
    create window with profile "Default"
    tell current session of current window
        set name to "Claude: official + dev"
        write text "export ANTHROPIC_API_KEY=xxx && claude"
    end tell
end tell
```

### 终端探测逻辑

```rust
fn detect_terminals() -> Vec<TerminalInfo> {
    let mut terminals = vec![];

    // Terminal.app 总是可用
    terminals.push(TerminalInfo {
        id: "terminal",
        name: "Terminal",
        enhanced: false,
    });

    // 检测 iTerm2
    if Path::new("/Applications/iTerm.app").exists() {
        terminals.push(TerminalInfo {
            id: "iterm2",
            name: "iTerm2",
            enhanced: true,
        });
    }

    terminals
}
```

## 开发计划

### Phase 0: 测试基础设施

- [ ] 配置 Vitest + 测试环境
- [ ] 为现有 CLI 核心模块编写单元测试：
  - [ ] `utils.ts` - 加密/解密函数
  - [ ] `presets.ts` - 预设配置
  - [ ] `permissions.ts` - 权限模式应用/重置
  - [ ] `usage.ts` - 用量统计解析
  - [ ] `skills.ts` - Skill 安装/卸载
  - [ ] `remote.ts` - 远程配置加载
- [ ] 集成测试：CLI 命令端到端测试
- [ ] CI 配置：每次提交运行测试

### Phase 1: 基础框架（MVP）

- [ ] Monorepo 结构搭建（pnpm workspace）
- [ ] 提取 `packages/core` 共享模块
- [ ] Tauri 项目初始化 + React + TailwindCSS + shadcn/ui
- [ ] 基础窗口 + 顶部 Tab 导航

### Phase 2: 核心功能

- [ ] 环境管理页面（CRUD）
- [ ] 权限模式页面（查看 + 切换）
- [ ] 配置持久化（复用 conf 或 Tauri store）
- [ ] 基础终端启动（Terminal.app）

### Phase 3: 系统托盘

- [ ] 托盘菜单实现
- [ ] 环境/权限快速切换
- [ ] 启动 Claude 入口

### Phase 4: 会话管理

- [ ] 活跃会话列表
- [ ] 进程状态监控
- [ ] iTerm2 增强集成

### Phase 5: 完善功能

- [ ] Dashboard 统计卡片
- [ ] 用量统计集成
- [ ] Skills 管理页面
- [ ] 设置页面（终端选择、主题等）

### Phase 6: 优化与发布

- [ ] macOS 原生风格打磨
- [ ] 开机启动
- [ ] 自动更新
- [ ] DMG 打包发布

## 测试策略

| 模块 | 测试类型 | 重点 |
|------|---------|------|
| `utils.ts` | 单元测试 | encrypt/decrypt 对称性 |
| `presets.ts` | 单元测试 | 预设完整性 |
| `permissions.ts` | 单元测试 + Mock FS | 配置文件读写正确性 |
| `usage.ts` | 单元测试 + Fixtures | JSONL 解析、费用计算 |
| `skills.ts` | 单元测试 + Mock FS | 安装路径、文件操作 |
| `config` (conf) | 集成测试 | 配置持久化 |
| CLI commands | E2E 测试 | 完整命令流程 |

**重构后验证：**

- 提取 `packages/core` 后，所有测试必须通过
- CLI 功能回归测试确保无破坏性变更
